<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CGV Attendance Checker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
            crossorigin="anonymous"></script>
</head>
<body>

<div class="container p-4 mt-2">
    <h3 class="row justify-content-md-center mb-3">CGV Attendance Checker</h3>
    <form method="post" enctype="multipart/form-data" action="/readimg">
        <div class="input-group mb-3">
            <input type="file" class="form-control" name="file">
            <input type="submit" value="Upload" class="input-group-text">
        </div>
    </form>
</div>

<div class="container p-4 mt-3">
    <% if(students.length > 0){ %>
        <table class="table">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Student Id</th>
                <th scope="col">Title</th>
                <th scope="col">Name</th>
                <th scope="col">Attendance</th>
                <th scope="col">Signature</th>
            </tr>
            </thead>
            <tbody>
            <% for(var i = 0; i < students.length; i++) { %>
                <tr>
                    <td><%= students[i][0] %></td>
                    <td><%= students[i][1] %></td>
                    <td><%= students[i][2] %></td>
                    <td><%= students[i][3] %></td>
                    <td><%= students[i][4] %></td>
                    <td>
                        <div class="container">
                            <div class="col-md-4 px-0">
                                <img class="img-fluid" src="<%= signatures[i] %>" alt="">
                            </div>
                        </div>
                    </td>
                </tr>
            <% } %>
            </tbody>
        </table>
    <% } %>
</div>

<% if(original_img !== ''){ %>
    <div class="container mt-3 p-4">
        <div class="row gx-5">
            <div class="col-md-6 section-img">
                <img src="<%= original_img %>" class=" img-fluid" alt=""/>
            </div>
            <div class="col-md-6 my-auto">
                <h1 class="section-heading">Original Image</h1>
                <p class="text-muted"></p>
            </div>
        </div>
    </div>
<% } %>

<% if(deskew_img !== ''){ %>
    <div class="container mt-3 p-4">
        <div class="row gx-5">
            <div class="col-md-6 section-img">
                <img src="<%= deskew_img %>" class=" img-fluid" alt=""/>
            </div>
            <div class="col-md-6 my-auto">
                <h1 class="section-heading">Deskew Image</h1>
                <p class="text-muted h4">
                    We deskew the image so the table will be axis aligned
                </p>
            </div>
        </div>
    </div>
<% } %>

<% if(grayscale_img !== ''){ %>
    <div class="container mt-3 p-4">
        <div class="row gx-5">
            <div class="col-md-6 section-img">
                <img src="<%= grayscale_img %>" class=" img-fluid" alt=""/>
            </div>
            <div class="col-md-6 my-auto">
                <h1 class="section-heading">Grayscale Image</h1>
                <p class="text-muted h4">
                    We then grayscale the image for binarization
                </p>
            </div>
        </div>
    </div>
<% } %>

<% if(binary_img !== ''){ %>
    <div class="container mt-3 p-4">
        <div class="row gx-5">
            <div class="col-md-6 section-img">
                <img src="<%= binary_img %>" class=" img-fluid" alt=""/>
            </div>
            <div class="col-md-6 my-auto">
                <h1 class="section-heading">Binary Image</h1>
                <p class="text-muted h4">
                    The binarized image
                </p>
            </div>
        </div>
    </div>
<% } %>

<% if(binary_inv_img !== ''){ %>
    <div class="container mt-3 p-4">
        <div class="row gx-5">
            <div class="col-md-6 section-img">
                <img src="<%= binary_inv_img %>" class=" img-fluid" alt=""/>
            </div>
            <div class="col-md-6 my-auto">
                <h1 class="section-heading">Binary Image Inverted</h1>
                <p class="text-muted h4">
                    We invert the binary image
                </p>
            </div>
        </div>
    </div>
<% } %>

<% if(vl_img !== ''){ %>
    <div class="container mt-3 p-4">
        <div class="row gx-5">
            <div class="col-md-6 section-img">
                <img src="<%= vl_img %>" class=" img-fluid" alt=""/>
            </div>
            <div class="col-md-6 my-auto">
                <h1 class="section-heading">Extracted Vertical Lines</h1>
                <p class="text-muted h4">
                    We create a kernel to extract vertical lines of the binary image
                </p>
            </div>
        </div>
    </div>
<% } %>

<% if(hl_img !== ''){ %>
    <div class="container mt-3 p-4">
        <div class="row gx-5">
            <div class="col-md-6 section-img">
                <img src="<%= hl_img %>" class=" img-fluid" alt=""/>
            </div>
            <div class="col-md-6 my-auto">
                <h1 class="section-heading">Extracted Horizontal Lines</h1>
                <p class="text-muted h4">
                    We create a kernel to extract horizontal lines of the binary image
                </p>
            </div>
        </div>
    </div>
<% } %>

<% if(vh_img !== ''){ %>
    <div class="container mt-3 p-4">
        <div class="row gx-5">
            <div class="col-md-6 section-img">
                <img src="<%= vh_img %>" class=" img-fluid" alt=""/>
            </div>
            <div class="col-md-6 my-auto">
                <h1 class="section-heading">Combined Image</h1>
                <p class="text-muted h4">
                    We combine the horizontal and vertical line images to get the image with table boundary
                </p>
            </div>
        </div>
    </div>
<% } %>

<% if(vh_binary !== ''){ %>
    <div class="container mt-3 p-4">
        <div class="row gx-5">
            <div class="col-md-6 section-img">
                <img src="<%= vh_binary %>" class=" img-fluid" alt=""/>
            </div>
            <div class="col-md-6 my-auto">
                <h1 class="section-heading">Combined Image Binarized </h1>
                <p class="text-muted h4">
                    We binarize the combined table boundary image
                </p>
            </div>
        </div>
    </div>
<% } %>

<% if(img_cnt !== ''){ %>
    <div class="container mt-3 p-4">
        <div class="row gx-5">
            <div class="col-md-6 section-img">
                <img src="<%= img_cnt %>" class=" img-fluid" alt=""/>
            </div>
            <div class="col-md-6 my-auto">
                <h1 class="section-heading">Image Bounding boxes</h1>
                <p class="text-muted h4">
                    We use the combined image to find all contours then sort them from top to bottom and then map the
                    bounding box of each cell of the table
                </p>
            </div>
        </div>
    </div>
<% } %>

<div class="container p-4 mt-3">
    <% if(table_cells.length > 0) { %>
        <table class="table">
            <thead>
            <tr>
                <th scope="col">Image</th>
                <th scope="col">Label</th>
            </tr>
            </thead>
            <tbody>
            <% for(var j = 0; j < (table_cells.length); j++) { %>
                <tr>
                    <td>
                        <div class="container">
                            <div class="col-md-4 p-2">
                                <img class="img-fluid h-100 w-100" src="<%= table_cells[j][1] %>" alt="">
                            </div>
                        </div>
                    </td>
                    <td><h4><%= table_cells[j][0] %></h4></td>
                </tr>
            <% } %>
            </tbody>
        </table>
    <% } %>
</div>



</body>
</html>
